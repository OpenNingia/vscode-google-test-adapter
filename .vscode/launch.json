{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "extensionHost",
      "request": "launch",
      "name": "Google adapter",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/out"
      ]
    },
    {
      "type": "node",
      "request": "attach",
      "name": "Google worker",
      "processId": "${command:PickProcess}"
    }
  ]
}
